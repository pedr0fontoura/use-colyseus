{
  "name": "server",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --rs --respawn -r tsconfig-paths/register src/index.ts",
    "check": "tsc --noEmit",
    "format": "eslint . --fix",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^20.6.2",
    "ts-node-dev": "^2.0.0",
    "tsconfig-paths": "^4.2.0"
  },
  "dependencies": {
    "colyseus": "^0.15.8",
    "express": "^4.18.2"
  }
}
