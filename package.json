{
  "name": "monorepo",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@colyseus/schema": "^2.0.14"
  },
  "devDependencies": {
    "typescript": "^5.0.2"
  }
}
